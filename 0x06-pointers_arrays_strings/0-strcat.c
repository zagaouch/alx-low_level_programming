#include "main.h"
/*
 * _strcat - function that concatenates two strings
 * @src : string source
 * @dest :  string desination
 */
char *_strcat(char *dest, char *src)
{
	char *c = dest;

	while(*c)
	{
		c++;
	}
	while(*src)
	{
		*p = *src;
		p++;
		src++;
	}
	*p = '\0';
	return (dest);
}
